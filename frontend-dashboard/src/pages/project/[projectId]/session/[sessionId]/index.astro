---
import Layout from '../../../../../layouts/Layout.astro';
import Logs from '../../../../../components/Session/Logs';

export interface Props {}

const { sessionId, projectId } = Astro.params;
---

<Layout title={`Spot - Session (${sessionId})`}>
	<Logs client:load projectId={projectId} sessionId={sessionId} />
</Layout>
