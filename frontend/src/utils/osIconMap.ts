import { FaWindows } from 'react-icons/fa';
import { SiMacos } from 'react-icons/si';

const osIconMap: Record<string, any> = {
	'Microsoft Windows': FaWindows,
	macos: SiMacos
};

export default osIconMap;
